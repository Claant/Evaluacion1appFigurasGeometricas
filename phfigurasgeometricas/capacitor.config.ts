import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.phfigurasgeometricas',
  appName: 'phfigurasgeometricas',
  webDir: 'www'
};

export default config;
